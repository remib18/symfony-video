<form method="post" action="{{ path('app_website_settings_delete', {'id': website_setting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ website_setting.id) }}">
    <button class="btn">Delete</button>
</form>
